diff --git a/site/src/layouts/BaseLayout.astro b/site/src/layouts/BaseLayout.astro
index 9b39f76..3713744 100644
--- a/site/src/layouts/BaseLayout.astro
+++ b/site/src/layouts/BaseLayout.astro
@@ -8,14 +8,11 @@ interface Props {
   title?: string;
   description?: string;
   activePage?: string;
-  /** Home page uses the 4/8 grid with inline navigation; inner pages use a single-column gallery container */
-  variant?: 'home' | 'inner';
 }
 const {
   title = 'Onovich',
   description = 'Onovich — creates games on a sofa.',
   activePage = '',
-  variant = 'inner',
 } = Astro.props;
 
 const navLinks = [
@@ -36,6 +33,7 @@ const navLinks = [
 // blank line: content categories (CODES…POEMS) and outbound links (SNS…MESSAGE).
 const navMain   = navLinks.slice(0, 8);
 const navSocial = navLinks.slice(8);
+const isHome    = activePage === '/';
 ---
 <!doctype html>
 <html lang="zh-CN">
@@ -46,7 +44,7 @@ const navSocial = navLinks.slice(8);
     <title>{title}</title>
     <link rel="icon" type="image/x-icon" href="/favicon.ico" />
   </head>
-  <body class={`variant-${variant}`}>
+  <body>
     <!-- Floating hamburger button (top-right) -->
     <button class="site-menu-button" id="siteMenuBtn" aria-label="Open menu">≡</button>
 
@@ -69,42 +67,50 @@ const navSocial = navLinks.slice(8);
     <main class="container">
       <div class="container_width">
         <bodycopy class="bodycopy content content_padding">
-          {variant === 'home' ? (
-            <div class="page_content home-grid">
-              <div class="home-grid__col home-grid__col--nav">
-                <h1><a href="/">Onovich</a></h1>
-                <h2 class="home-nav">
-                  {navMain.map((link, i) => (
-                    <>
-                      <a href={link.href}>{link.label}</a>
-                      {i < navMain.length - 1 && <br />}
-                    </>
-                  ))}
-                </h2>
-                <h2 class="home-nav home-nav--social">
-                  {navSocial.map((link, i) => (
-                    <>
-                      <a href={link.href}>{link.label}</a>
-                      {i < navSocial.length - 1 && <br />}
-                    </>
-                  ))}
-                </h2>
-              </div>
-              <div class="home-grid__col home-grid__col--main">
-                <slot />
-              </div>
+          <div class="page_content home-grid">
+            <!-- LEFT COL — site nav (rendered on every page, mirrors original Cargo pinned overlay) -->
+            <div class="home-grid__col home-grid__col--nav">
+              <h1><a href="/">Onovich</a></h1>
+              <h2 class="home-nav">
+                {navMain.map((link, i) => (
+                  <>
+                    <a
+                      href={link.href}
+                      class={activePage === link.href ? 'active' : ''}
+                    >{link.label}</a>
+                    {i < navMain.length - 1 && <br />}
+                  </>
+                ))}
+              </h2>
+              <h2 class="home-nav home-nav--social">
+                {navSocial.map((link, i) => (
+                  <>
+                    <a
+                      href={link.href}
+                      class={activePage === link.href ? 'active' : ''}
+                    >{link.label}</a>
+                    {i < navSocial.length - 1 && <br />}
+                  </>
+                ))}
+              </h2>
             </div>
-          ) : (
-            <div class="page_content">
+
+            <!-- RIGHT COL — page-specific content -->
+            <div class="home-grid__col home-grid__col--main">
+              {!isHome && (
+                <div class="page-back">
+                  <a href="/">&lt; HOME</a>
+                </div>
+              )}
               <slot />
             </div>
-          )}
+          </div>
         </bodycopy>
       </div>
     </main>
 
     <footer class="site-footer">
-      <span class="build-version">v2.2.0-astro · 2026-04-29</span>
+      <span class="build-version">v2.3.0-astro · 2026-04-29</span>
     </footer>
 
     <script>
diff --git a/site/src/pages/index.astro b/site/src/pages/index.astro
index b3f9520..f68f260 100644
--- a/site/src/pages/index.astro
+++ b/site/src/pages/index.astro
@@ -1,7 +1,7 @@
 ---
 import BaseLayout from '../layouts/BaseLayout.astro';
 ---
-<BaseLayout title="Onovich" activePage="/" variant="home">
+<BaseLayout title="Onovich" activePage="/">
   <img class="home-avatar" src="/images/profile/avatar.jpg" alt="Onovich" />
   <hr class="home-divider" />
   <div class="home-bio">
diff --git a/site/src/styles/global.css b/site/src/styles/global.css
index bb5e936..03ca958 100644
--- a/site/src/styles/global.css
+++ b/site/src/styles/global.css
@@ -147,8 +147,21 @@ small {
   line-height: 1.9;
 }
 .home-nav a { color: rgba(67, 67, 67, 0.85); }
+.home-nav a.active { opacity: 0.4; }
 .home-nav--social { margin-top: 1.5rem; }
 
+/* Inner page "< HOME" back link, top-right of right column */
+.page-back {
+  text-align: right;
+  margin-bottom: 1.5rem;
+}
+.page-back a {
+  font-family: var(--font-zh);
+  font-size: 1.2rem;
+  color: rgba(67, 67, 67, 0.85);
+}
+.page-back a:hover { opacity: 0.55; }
+
 .home-avatar {
   width: 72px;
   height: 72px;
@@ -177,8 +190,8 @@ small {
    GALLERY (Cargo .thumbnails)
    ============================================================ */
 .thumbnails-container {
-  width: var(--thumbnails-width);
-  margin: 0 auto 0 0;
+  width: 100%;
+  margin: 0;
 }
 
 .thumbnails {
